# Docker Compose - odoo stinger conceptsun97139
# Généré automatiquement depuis manifest Phase 1
# Source attendue: odoo.stinger.conceptsun97139
# DB name: odoo_stinger_conceptsun97139
# Compose project: dorevia_odoo_stinger_conceptsun97139

services:
  db:
    image: postgres:16
    container_name: odoo_db_stinger_conceptsun97139
    restart: unless-stopped
    environment:
      POSTGRES_DB: odoo
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
    volumes:
      - odoo_stinger_conceptsun97139_db:/var/lib/postgresql/data
    networks:
      - dorevia-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odoo"]
      interval: 10s
      timeout: 5s
      retries: 5

  odoo:
    image: odoo:18.0-20250819
    container_name: odoo_stinger_conceptsun97139
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    networks:
      - dorevia-network
    volumes:
      # Sources OCA (read-only)
      - /opt/dorevia-plateform/sources/oca:/mnt/oca:ro
      # Extra-addons flatten (symlinks)
      - oca_extra_addons:/mnt/extra-addons
      # Addons métier
      - /opt/dorevia-plateform/units/odoo/custom-addons:/mnt/custom-addons
      # Config Odoo
      - ./odoo.conf:/etc/odoo/odoo.conf:ro
      # Filestore
      - odoo_stinger_conceptsun97139_data:/var/lib/odoo
    # Exécuter oca_flatten.sh puis démarrer Odoo
    command: ["sh", "-c", "/mnt/custom-addons/bin/oca_flatten.sh && odoo -c /etc/odoo/odoo.conf"]
    # Pas de port exposé (routage via Caddy uniquement)

volumes:
  odoo_stinger_conceptsun97139_db:
    name: odoo_stinger_conceptsun97139_db
  odoo_stinger_conceptsun97139_data:
    name: odoo_stinger_conceptsun97139_data
  oca_extra_addons:
    name: oca_extra_addons

networks:
  dorevia-network:
    external: true
