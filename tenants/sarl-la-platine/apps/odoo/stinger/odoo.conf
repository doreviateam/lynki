# Configuration Odoo odoo stinger sarl-la-platine
# Template généré automatiquement par dorevia.sh
# Source attendue: odoo.stinger.sarl-la-platine
# DB name: odoo_stinger_sarl-la-platine

[options]
# Base de données
db_host = odoo_db_stinger_sarl-la-platine
db_port = 5432
db_user = odoo
db_password = odoo
dbfilter = ^odoo_stinger_sarl-la-platine$

# Mot de passe maître pour le gestionnaire de base de données
admin_passwd = doreviateam@2026

# Addons
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/mnt/extra-addons,/mnt/custom-addons

# Data
data_dir = /var/lib/odoo

# Reverse proxy (Caddy)
proxy_mode = True

# Queue Job (SPEC Orchestration temps réel DVIG via queue_job Odoo v1.0)
server_wide_modules = web,queue_job
workers = 2

# Logs (optionnel)
# logfile = /var/log/odoo/odoo.log
# log_level = info

[queue_job]
# root + canal vault (Reconciler proof / fetch_proof)
channels = root:2,dorevia_vault:2

