# Copier en .env et renseigner les valeurs (ne pas committer .env)
# Les deux doivent être définis : ils sont injectés dans la config Odoo au démarrage.
POSTGRES_PASSWORD=<mot_de_passe_fort_db>
ADMIN_PASSWD=<mot_de_passe_maitre_odoo>
